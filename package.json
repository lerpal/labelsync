{
  "devDependencies": {
    "eslint": "^8.55.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.0",
    "web-ext": "^7.9.0"
  },
  "scripts": {
    "build:firefox": "web-ext build --artifacts-dir=firefox-builds --ignore-files ./package.json ./package-lock.json"
  }
}
